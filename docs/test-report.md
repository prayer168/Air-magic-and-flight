# 測試報告

教材：空氣魔法與飛行互動實驗室（v2.0.0，單一 index.html）
測試日期：2026-07-19
環境：Windows 11、Chromium（Claude Code Browser pane）、本機 `python -m http.server 8620`

## 環境限制說明

本次測試期間，Browser pane 的**截圖服務逾時失效**（`computer{screenshot}` 一律 30s timeout，連載入 data: 頁面亦被阻擋），但頁面本身對 JavaScript、文字擷取與版面量測完全正常回應。因此以 **DOM 幾何量測、computed style 與行為驗證** 取代像素截圖進行視覺相關檢查；og 圖經 PNG 解碼目視確認。部署後將於公開網址重試截圖。

## 版面（水平溢位）驗證

以程式對 16 章逐一啟用並量測 `scrollWidth - clientWidth` 及各元素右緣是否超出視窗：

| 視窗 | 結果 |
|---|---|
| 桌機 1440×1000 | ✅ 16 章全部無水平溢位，無元素超出視窗；hero/實驗版面正常 |
| 平板 768×1024 | ✅ 16 章無溢位；`.grid2`（控制項/舞台）正確收合為單欄 |
| 手機 390×844 | ✅ 16 章無溢位、無超寬元素（符合「手機不得出現水平捲軸」） |

## 功能驗證（實際操作，共 10 模擬）

以程式對每個實驗按發射，讀取指標與場景最終狀態：

- **紙飛機**：距離/滯空/最高/穩定性/落點皆輸出合理值；plane 位移、trail 軌跡繪出。✅
- **吹管**：出口速度、距離、與目標差距、命中；預設超前提示調整。✅（安全提醒完整）
- **氣球火箭**：距離/最高速/到達率；氣球隨氣量脹縮、噴氣、作用力反作用力箭頭。✅
- **空氣砲**：射程/穩定度/吹倒紙杯；力道 90% 時杯翻倒（transform rotate 70）。✅
- **降落傘**：下降時間/速度/飄移/穩定；傘體下降至地面、隨風飄移。✅
- **紙飛旋翼**：旋轉圈數/滯空/下降速度；葉片旋轉（rotate 5400°）；同向折幾乎不轉。✅
- **伯努利漂浮球**：預設穩定漂浮 68 cm（校準後）；可拖曳、放手回中或掉落。✅
- **飛行種子**：飄浮/旋轉/滑翔三型；下降＋飄移＋旋轉。✅
- **風帆車**：速度/到終點時間；到達終點；帆太大又太輕會變慢之提示。✅
- **機翼升力**：升力/阻力/是否飛得起來/失速；8 個受力箭頭與 5 條氣流線；超過失速角顯示失速。✅

指標於**按發射當下即提交**（動畫僅視覺呈現），故即使動畫在背景被節流仍正確記錄。

## 探究流程與其他功能

- 公平測試偵測：一次改兩個變因再發射 → 跳出提醒並列出改動的變因名稱。✅
- 探究紀錄表：輸入數據自動繪長條圖（2 筆→2 長條）；「帶到紀錄表」由實驗帶入研究問題、自變因、表頭。✅
- 儲存／下載文字檔／複製／列印／清除；LocalStorage 保存（amf2_done/record/mode/theme/noAnim）。✅
- 迷思卡翻面、8 則生活應用、15 題評量（答對計分、答錯標示正解並鎖定、附原因）、11 題答對顯示證書。✅
- 基礎／進階模式：adv-only 於基礎隱藏、進階顯示。✅　深淺色：背景色隨主題改變。✅
- 鍵盤：章節列 ArrowRight 移動焦點到下一章。✅　學習進度條隨完成章節更新（13/16→81.25%）。✅
- 主控台錯誤：無。

## 已知限制

- 十個模擬數值為簡化模型「模擬結果」，非實測值（已於教材標示）。
- 本輪未能取得像素截圖（環境截圖服務逾時）；改以幾何/行為驗證，部署後於公開網址重試。
- 減少動態模式下動畫直接跳至最終狀態（程式路徑已驗證）。

## 公開部署驗證（2026-07-19）

- 網址：https://prayer168.github.io/Air-magic-and-flight/
- 首頁 HTTP 200、標題含「空氣魔法與飛行互動實驗室」、含 16 個章節節點與 og:image 標籤。✅
- og 圖 `img/og/Air-magic-and-flight.png` HTTP 200（656,797 bytes，十實驗版）。✅
- 公開版手機 390×844 實測：無水平溢位；機翼實驗迎角 20° 顯示「失速」、空氣砲射程正常；主控台無錯誤。✅
- 像素截圖因環境截圖服務逾時未能取得（公開網址亦同），確認為環境問題而非頁面問題。
